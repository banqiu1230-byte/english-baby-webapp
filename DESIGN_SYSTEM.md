# Luma Mobile Design System v1.1

这套规范服务于“在真实场景中自然学会英语”的手机端产品。外层采用温暖的生活地图与经历册，内层保留真实情境和语音驱动的画面变化。

## 1. 设计原则

1. **功能先于装饰**：标题直接说明用户能做什么，例如“选择一个场景，直接开始对话”。
2. **场景是主角**：首页用真实情境图承载主要入口；普通功能页保持干净，让内容容易扫读。
3. **表面只选一种表达**：有底色的卡片不再加描边；只有透明或输入类组件才允许描边。
4. **说话状态必须明确**：待机、按住、聆听、理解、回应、完成均有不同文字和状态色。
5. **手机单手可用**：主要操作不小于 48px，高频按钮靠近底部，圆角和间距保持稳定。

## 2. 三层 Token

### Primitive

| 类型 | Token | 值 |
|---|---|---|
| 深文字 | `--ink` | `#1E1D26` |
| 次文字 | `--muted` | `#756F68` |
| 页面底色 | `--paper` | `#F7F2E9` |
| 主要表面 | `--surface` | `#FFFDF8` |
| 紫色强调 | `--violet` | `#806EE8` |
| 深紫强调 | `--violet-deep` | `#6653CE` |
| 暖色分隔 | `--warm-line` | `rgba(83, 70, 54, .12)` |
| 圆角 | 小／中／大 | `15 / 20 / 30px` |

### Semantic

- `--paper`：旅程、世界、学习笔记和设置的暖白画布。
- `--surface`：白色主要表面。
- `--surface-subtle`：设置项、标签等低强调表面。
- `--ink` / `--muted`：主次文本。
- 深色主按钮：当前事件唯一主动作；紫色只标记当前位置与选中状态。
- `--success`：完成、在线、已理解。

### Component

- 卡片：`--card-bg`、`--card-radius`、`--card-shadow`。
- 主按钮：`--button-bg`、`--button-fg`、`--button-radius`。
- 底部导航：`--dock-bg`、`--dock-selected-bg`。

实际值以 `styles.css` 中的 `Luma Mobile Design System v1.0` 区块为准。

## 3. 字体与层级

- 字体栈：Avenir Next → PingFang SC → SF Pro。
- 页面主标题：29–32px / 700，紧凑字距。
- 区块标题：18–22px / 680。
- 卡片标题：14–28px / 650–720，按层级调整。
- 正文：12–14px；辅助信息不得低于 11px。任何可见文字、状态、标签、按钮说明和对话署名都以 11px 为绝对下限。
- 英文不再使用杂志式衬线体，避免产品像内容海报而不像学习工具。

## 4. 布局与间距

- 手机内容左右安全边距：20px；360px 短屏使用 16px。
- 主要区块间距：24–32px。
- 卡片内部间距：20–24px。
- 同组元素间距：8–16px。
- 底部导航固定在安全区上方，每项点击高度至少 54px。
- 世界页是全幅地图例外：地图占满底栏以上空间，不使用普通页面边距、圆角、阴影或纵向内容流。
- 世界页顶部功能标题和底部当前旅程使用地图内浮层；底部旅程模块高度控制在 70px，地图主页面不再展开 C01–C04 长列表。

## 5. 组件规范

### 主按钮

- 近黑填充、白字，箭头使用克制的淡紫色，胶囊圆角。
- 默认高度 54px；360×640 可降到 50px。
- 按下时颜色加深并缩放至 99%。
- 禁止同时出现蓝色底和蓝色描边。

### 信息卡片

- 默认白底或单一浅色底；只允许低对比度分隔线说明结构。
- 普通列表不使用阴影；重点入口才使用冷色轻阴影。
- 圆角统一为 20px 或 28px，禁止混用不对称切角。

### 底部导航

- 暖白固定底栏，三等分，不使用浮动第四入口。
- 选中项图标和文字变为紫色，并提高字重。
- 标签固定为：旅程、世界、笔记；设置从右上角头像进入。

### 语音按钮

- 进入场景时，先播放 Luma 的动态开场；实际播放完毕后自动开始持续聆听。
- 任务只由相关语音回应推进，不需要按住、手动发送、点击答案或拖动物体。
- 麦克风按钮只负责“静音 / 恢复”，状态文字明确说明正在听、正在回应或需要恢复权限。
- 人物说话时继续采集；用户若开口，系统在人物完整一句结束后承接，不用重新说一遍。
- 物体和订单负责呈现理解结果，不承担通关操作。

## 6. 场景页例外

场景页是沉浸层，可以使用深色图像遮罩、玻璃控制按钮与白色字幕。它不继承普通页面的白卡片语言，但仍遵循：

- 学习者开口前不显示参考答案。
- 人物问题、学习者自由回答和画面结果连续发生。
- Luma 的模型输出逐字驱动字幕，不使用固定台词冒充实时对话。
- 对话使用左右气泡：Luma 在左，用户在右；画面只保留最近两条，超过两条提供“全部对话”底部面板。
- 用户原话作为“你”的字幕显示；只有检测到上下文错误或确实不自然时，才在原话下展示更合适的回应。正确的自然说法不得为了变化而重写。
- 只有用户说完后才显示自然改写。
- 场景主图使用完整显示（contain），背景用同图模糊铺满；交互坐标必须按主图的实际缩放与留白映射，禁止按 cover 裁切后的估算坐标。
- 不给物体添加白描边、外发光、脉冲圈或可点击热点框；需要强调时只改变旁边的单词标签。
- 单张人物素材允许使用轻量嘴部开合和呼吸光晕增强生命感，但不能移动整张背景导致物体热点错位。
- 纠错信息直接包含在用户气泡中，不另加抢占画面的浮层。

## 7. 禁止项

- 不使用“有底色 + 明显描边”的双重容器表达。
- 不使用装饰性英文眉题代替中文功能说明。
- 不使用大量不同形状的圆角或切角。
- 不照搬其他产品的角色、吉祥物、品牌渐变或商业功能。
- 不在用户开口前显示唯一答案或标准句。

## 8. 外层信息架构

- **旅程**回答“我当前正在攻略什么”：它是世界中一个分支的详情，只呈现当前地点、当前委托和继续入口，不重复展示整条路线。
- **世界**回答“我在哪里、接下来能去哪”：使用空间地图同时呈现当前旅程进度、可预览地点和未来分支；地图节点只打开预览，详情 CTA 才进入任务。
- **笔记**回答“今天复习什么、哪里还要多练、已经留下了什么成果”：依据真实练习记录整理到期复习、帮助条件、独立表达、跨场景复现和最近记录，不用虚构百分比或发音／语法结论替代能力证据。
- 世界地图的图片负责空间氛围，节点、名称、进度和状态必须由可访问 DOM 呈现，不能烙进图片。
- 世界主页面只保留地点总览与一个当前事件入口；任务分步细目进入预览或任务内部，避免地图再次退化为列表页。
