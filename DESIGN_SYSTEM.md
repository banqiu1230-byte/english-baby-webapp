# Luma Mobile Design System v1.0

这套规范服务于“在真实场景中自然学会英语”的手机端产品。视觉参考现代口语学习产品的清晰、轻盈与高反馈感，但保留 Luma 的真实情境和 2.5D 交互作为产品识别点。

## 1. 设计原则

1. **功能先于装饰**：标题直接说明用户能做什么，例如“选择一个场景，直接开始对话”。
2. **场景是主角**：首页用真实情境图承载主要入口；普通功能页保持干净，让内容容易扫读。
3. **表面只选一种表达**：有底色的卡片不再加描边；只有透明或输入类组件才允许描边。
4. **说话状态必须明确**：待机、按住、聆听、理解、回应、完成均有不同文字和状态色。
5. **手机单手可用**：主要操作不小于 48px，高频按钮靠近底部，圆角和间距保持稳定。

## 2. 三层 Token

### Primitive

| 类型 | Token | 值 |
|---|---|---|
| 主蓝 | `--blue-500` | `#3478F6` |
| 深蓝 | `--blue-600` | `#2868E8` |
| 浅蓝 | `--blue-50` | `#EDF4FF` |
| 深文字 | `--navy-950` | `#071A37` |
| 次文字 | `--slate-500` | `#919BAC` |
| 页面底色 | `--slate-100` | `#F1F4F8` |
| 紫色强调 | `--violet-500` | `#725CF6` |
| 珊瑚强调 | `--coral-500` | `#F56F86` |
| 成功绿 | `--mint-500` | `#13C98A` |
| 圆角 | `--radius-sm/md/lg` | `14 / 20 / 28px` |

### Semantic

- `--paper`：所有普通页面的冷白灰画布。
- `--surface`：白色主要表面。
- `--surface-subtle`：设置项、标签等低强调表面。
- `--ink` / `--muted`：主次文本。
- `--primary` / `--primary-soft`：核心动作与其浅色容器。
- `--success`：完成、在线、已理解。

### Component

- 卡片：`--card-bg`、`--card-radius`、`--card-shadow`。
- 主按钮：`--button-bg`、`--button-fg`、`--button-radius`。
- 底部导航：`--dock-bg`、`--dock-selected-bg`。

实际值以 `styles.css` 中的 `Luma Mobile Design System v1.0` 区块为准。

## 3. 字体与层级

- 字体栈：Avenir Next → PingFang SC → SF Pro。
- 页面主标题：34–38px / 800，紧凑字距。
- 区块标题：22–24px / 800。
- 卡片标题：18–28px / 800，按卡片尺寸调整。
- 正文：12–14px；辅助信息 10–11px。
- 英文不再使用杂志式衬线体，避免产品像内容海报而不像学习工具。

## 4. 布局与间距

- 手机内容左右安全边距：22px。
- 主要区块间距：32–40px。
- 卡片内部间距：20–24px。
- 同组元素间距：8–16px。
- 底部导航悬浮于安全区上方，点击高度至少 52px。

## 5. 组件规范

### 主按钮

- 电光蓝填充、白字、胶囊圆角。
- 默认高度 54px。
- 按下时颜色加深并缩放至 99%。
- 禁止同时出现蓝色底和蓝色描边。

### 信息卡片

- 默认白底或单一浅色底，无描边。
- 普通列表不使用阴影；重点入口才使用冷色轻阴影。
- 圆角统一为 20px 或 28px，禁止混用不对称切角。

### 筛选项

- 未选中：白底、深灰字、无描边。
- 选中：深海军蓝底、白字。

### 底部导航

- 白色浮动胶囊容器。
- 选中项使用浅灰蓝胶囊底，图标和文字变为主蓝。
- 标签固定为：首页、场景、成长、我的。

### 语音按钮

- 进入场景时，先播放 Luma 的动态开场；实际播放完毕后自动开始持续聆听。
- 用户可以一边说话一边拖动物体，不需要按住或手动发送。
- 麦克风按钮只负责“静音 / 恢复”，蓝色呼吸状态代表正在聆听。
- Luma 开口后麦克风仍持续工作；用户可以随时插话，系统检测到用户声音后立即暂停 Luma 并转为聆听。
- 状态文案必须明确写出“随时可以打断”，不要求用户猜测轮次。

## 6. 场景页例外

场景页是沉浸层，可以使用深色图像遮罩、玻璃控制按钮与白色字幕。它不继承普通页面的白卡片语言，但仍遵循：

- 学习者开口前不显示参考答案。
- 人物问题、学习者自由回答和物体动作并行发生。
- Luma 的模型输出逐字驱动字幕，不使用固定台词冒充实时对话。
- 对话使用左右气泡：Luma 在左，用户在右；画面只保留最近两条，超过两条提供“全部对话”底部面板。
- 用户原话作为“你”的字幕显示；只有检测到上下文错误或确实不自然时，才在原话下展示更合适的回应。正确的自然说法不得为了变化而重写。
- 只有用户说完后才显示自然改写。
- 场景主图使用完整显示（contain），背景用同图模糊铺满；交互坐标必须按主图的实际缩放与留白映射，禁止按 cover 裁切后的估算坐标。
- 物体高亮直接贴合物体轮廓或有效点击区，不能漂在物体旁边。
- 单张人物素材允许使用轻量嘴部开合和呼吸光晕增强生命感，但不能移动整张背景导致物体热点错位。
- 纠错信息直接包含在用户气泡中，不另加抢占画面的浮层。

## 7. 禁止项

- 不使用“有底色 + 明显描边”的双重容器表达。
- 不使用装饰性英文眉题代替中文功能说明。
- 不使用大量不同形状的圆角或切角。
- 不照搬其他产品的角色、吉祥物、品牌渐变或商业功能。
- 不在用户开口前显示唯一答案或标准句。
