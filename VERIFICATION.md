# Luma 验证范围与历史记录

## 2026-09-18 全场景对话恢复

- 复现并修正两类永久等待：角色开始回复清掉 ASR 结束计时器，导致已听清的回答永远不进入判定；音频结束后等待 `AudioContext.resume()` 队列，所有看门计时器均被撤掉。现在转写按 item 独立结算，播放队列有 8 秒恢复期限。
- 有效回答统一取消已过时的自由回复，再播放短确认并进入下一步。覆盖早餐、机场、办公室及咖啡。任务完成时释放排队事件；旧任务/旧连接的迟到转写、取消和播放结果不能抢占新任务。
- `response.done` 无音频、当前回复被取消、字幕缺失、音频时钟停顿均进入可恢复分支。连续不结束的角色回复上限缩短为 30 秒。失败后保留文字与可继续说话/提示入口，不把网络异常算学习错误，不把无关话直接算任务成功。
- 两个截图原句纳入回归：早餐的 `uh huh yes hear you` 仅在实际索要杯子的语境中识别为应允；机场的 `Uh nothing yes it's my bag` 识别明确所有权。只说“我听见了”、求助、疑问、不确定与矛盾回答有反例验证。
- 自动化使用真实生产函数、模拟传输和设备事件；不是对任意真人口音、每种手机音频实现或外部服务可靠性的保证。浏览器与真实服务的证据分开记录，见 `qa-evidence/`。
- 最终 `npm test` **218/218**、语法检查、`git diff --check` 通过。CUA 操作隔离 QA 页的真实界面，早餐/机场的原句在缺 ASR final 条件下约 6 秒确认、约 9.4 秒已到下一步；办公室无关句与断音约 8 秒恢复，保持本题未通过。三个页面均麦克风可用且可见，无 JS 错误。结果：`qa-evidence/all-scene-browser-results.json`。
- 单次真实语音接口使用合成音频，ASR 产生额外前缀 `Are nothing. Yes, it is my bag.`，本地快判没有通过；单独调用真实 `/api/feedback` 返回有效意思。该次 WS 未执行 cancel/确认阶段，因此不作为真人麦克风或完整真实语音闭环通过的证据。记录：`qa-evidence/airport-recovery-live-events.json`。
- 美式堂食交付新增 small/large 单杯黑咖啡素材，CUA 实际加载两张图片并与任务文字核对；不再用双杯选择图。机场首页已目视验证无孤立咖啡路线、无残留 C04。素材映射测试验证按实际交付订单取图。此次没有重做其他拿铁/外带杯的全尺寸素材。

## 2026-09-17 旅程／世界／学习笔记

- 底栏现为“旅程／世界／笔记”。旅程页保留此前的街角咖啡店沉浸式分支详情；世界负责全部生活分支；笔记负责复习、需加强表达、学习成果和最近记录。
- 学习笔记只读取本地真实练习证据：区分独立英语语音、借助提示、中文／混合语言、文字和意思未确认；技术错误不会进入“还要多练”。成果统计为完成场景去重数、独立说出次数、独立表达项及同一表达跨场景复现数，不生成发音、语法或掌握率结论。
- 单项“先看场景／再练一次”只打开场景预览。隔离浏览器实测 `sceneSheet` 打开、`experience` 仍隐藏、麦克风请求为 0；关闭后仍停留在学习笔记。点击“开始今日复习”先进入练习引导，用户确认前麦克风请求为 0。
- Headless Chrome 实测 320×568、360×640、390×844：页面无横向溢出，可见文字最小 11px，所有按钮命中高度至少 44px。320×568 首屏仍可看到页面标题、今日复习、主按钮和“还要多练”标题。
- 截图见 `qa-evidence/learning-notes-320x568.png`、`learning-notes-360x640.png`、`learning-notes-390x844.png`。三档均无 console error、page error 或资源失败。
- 今日到期项目只在卡内预览前 2 项，其余用数量说明并在开始后依次安排，避免真实的长复习队列把主按钮挤出首屏。
- `npm test` 165/165、`npm run check`、`git diff --check` 通过。上述结果验证数据归因、布局和入口行为，不代表真人麦克风、语音识别准确率、长期学习效果或留存已经验收。

## 2026-09-17 世界整屏地图

- “世界”现在是固定一屏的生活地图：地图铺满底部导航以上的全部空间，标题和说明成为地图内浮层，不再以普通内容页呈现。
- 已删除地图下方的 C01–C04 长进度列表。当前旅程压缩为地图底部 70px 小模块，动态显示 `X / 4`、下一事件代码、标题、状态和细进度条。
- 咖啡、早餐、机场、办公室和未来城市五个节点全部保留；节点与当前旅程模块都只打开场景预览，`experience` 保持隐藏，测试替身记录到的麦克风请求为 0。
- Headless Chrome 实测 320×568、360×640、390×844：地图舞台分别完整占据底栏以上 498、570、774px，所有浮层无几何碰撞、无横向溢出，可见文字最小 11px。截图见 `qa-evidence/world-fullscreen-320x568.png`、`world-fullscreen-360x640.png`、`world-fullscreen-390x844.png`。
- 页面控制台已清理无效 favicon 请求；最终 console error 为空。`npm test` 160/160、`npm run check` 与 `git diff --check` 通过。
- 本轮没有进入任务或接受麦克风权限，不将地图验收外推为真人语音、学习效果或留存效果证明。

## 2026-09-16 世界地图与对话防卡死收口

- “冒险”现在只表达当前委托：当前旅程、发生地点、任务目标和继续按钮；重复的多节点路线已经移除。
- “世界”改为全局生活地图：1086×1448 的正式地图素材承载咖啡中心、早餐、机场、办公室与未来城市分支；页头和咖啡节点同步显示真实 `X / 4` 进度，下方保留当前旅程的 C01–C04 明细。
- 世界节点点击只打开详情预览，不会改写当前场景或申请麦克风；只有详情 CTA 才进入任务。
- Codex in-app browser 实测 320×568、360×640、390×844：无横向溢出，地图节点无碰撞，可见文字最小 11px。320×568 的冒险主图已缩短，继续按钮完整位于固定底栏上方。
- 对话流新增三类衔接修复：自然求助不再触发原题机械复读；关闭提示后恢复沉默救援；任务致谢期间提前说出的、可明确归属下一步的咖啡答案会被保留并在下一题出现后确认一次。
- 场景开始、沉默、识别缺少 final、模型无回复、音频断流、字幕缺失、普通任务切换、最终复盘、重连和 checkpoint resume 均有确定性回归覆盖。AudioContext 时钟连续 8 秒不推进会自动释放人物回合；人物单回合设 90 秒绝对上限，连续小音频包不能无限占用对话。
- `npm test`：160/160 通过；`npm run check` 与 `git diff --check` 通过。
- `npm run test:browser` 因项目未安装 Playwright 无法执行。本轮没有接受麦克风权限；真实手机收音、供应商语音识别准确率与系统音频行为仍需设备说话验收。

## 2026-09-16 对话切换、地图标注与字号下限

- 修复咖啡任务跨步骤错位：完成饮品选择后只确认当前答案，画面切到杯型步骤后才询问 `small / large`，不会在 `1 / 4` 里提前问下一题，也不会重复提问。
- 裸 `Yes / No` 不会填写饮品、杯型或堂食方式；中文元问题只保留在自然对话中，不再触发本地机械复读。
- 首页路线增加连接与完成进度，节点向路线收拢，文字使用半透明底板；短屏重新排列节点，避免与当前位置卡重叠。
- 世界页标题改为直接说明操作：选择场景、查看路线和继续事件。
- CSS 和真实 DOM 全量检查后，可见文字最小字号为 11px；320×568、360×640、390×844 无横向溢出。
- `npm test`：154/154 通过；`npm run check` 与 `git diff --check` 通过。
- 本轮没有接受浏览器麦克风权限；对话修复由状态机、客户端编排和服务端提示回归覆盖，真人语音仍需设备验收。

## 2026-09-16 生活地图外层

- 主导航已改为“冒险／世界／足迹”，设置从头像进入。390×844 和 360×640 真实 in-app browser 均确认首页主目标、CTA 与三项底栏完整可见；世界、足迹和设置截图见 `qa-evidence/*-390x844.jpg`。
- 世界页点击地点或任务只打开 `sceneSheet`；实测 `experienceActive=false`，没有直接启动场景。只有详情 CTA 才会提交 scene／mission；首页继续仍走 `LumaExperience.startHome()`，保留 checkpoint、任务变式与帮助记录。
- 足迹“走过的经历／我会的事”切换只读取本地记录；设置页隐藏底栏，返回后恢复原 tab。上述外层浏览没有请求麦克风，也没有生成学习证据。
- 最终页面 console error 为空；视觉 source-of-truth、并排比较、修复记录和明确范围见 `design-qa.md`。
- 当前 `npm run check`、`git diff --check` 通过；`npm test` **150/150 通过**。新增检查保证新用户从可玩的咖啡 C01 开始，而不是落入无关外层场景。

## 2026-09-16 咖啡任务冒险版

- 当时 `npm run check`、`git diff --check` 通过；`npm test` **149/149 通过**。新增检查覆盖 C01–C04 任务状态、目标订单差异、自然改口、C03 修复追问、任务与变式证据、词语提示归因、复盘续关、恢复进度，以及移除后的文字入口不能再推进任务；当前总数见上节。
- CUA 在当前本地手机界面完成 C02：输入错误的“大杯拿铁带走”后没有通关，订单保留差异并要求改回小杯；修正后跳过已说清的“带走”，进入交付。完成页正确引向 C03，没有再跳去机场。
- CUA 完成 C03：初始画面同时显示“点了小杯／收到大杯”；只说 `No, this is wrong.` 后继续追问 `What size did you order?`；说 `Sorry, it should be smaller.` 后画面、交付和订单一起变为小杯，再进入道谢。
- C04 已确认默认隐藏字幕，人物台词区不显示；中文任务目标保留。用户主动打开完整示范后，界面明确提示本次会按“借助帮助完成”记录，复盘不会把它冒充为无提示独立表达。
- 四关开场现在保留更大的咖啡店画面，任务路线可滚动，开始按钮固定在手机底部；C01–C03 顺序解锁，C04 使用变化订单。所有任务都由语言回应推进，物品变化自动发生。
- 上述 C02、C03 流程是在移除入口前用文字注入验证任务逻辑，只作为当时的状态机记录。当前界面、早餐/咖啡客户端入口和语音代理均已移除文字提交；旧本地文字记录仅为历史兼容，不能推进新任务或计入口语。当前版本的真人麦克风、插话与识别准确率仍需真机验收。
- 手机页面复核保留物品旁英文单词标签，同时去掉牛奶、水、杯子等物体外沿的白色描边和发光；任务底部只保留重听、麦克风和提示，不再出现打字按钮或输入表单。
- 现有五张咖啡图能持续反映步骤、选择和错单修复，但尚未为三维八种订单组合各制作独立成品素材；这项仍需素材专项验收。
- 本版没有付费入口。当前记录只能说明模拟任务在已测条件下完成，不能证明学习效果优于其他产品，也不能直接宣称现实场景已经掌握。

## 2026-09-16 场景画面与点咖啡（此前基线）

- `npm run check`、`git diff --check` 通过；`npm test` **127/127 通过**。新增检查覆盖咖啡的四步顺序、短句与中文选择、求助不推进、过期结果隔离、文字不计口语、订单保存、画面随任务与选择变化、异步图片加载隔离。
- CUA 实际页面验证：早餐 Water → Here you are → No, thanks 完成；咖啡拿铁 → 小杯 → 外带 → Thank you 完成（含第二步刷新续练）；另一条美式 → 大杯 → 堂食 → Thanks 完成。两条咖啡回顾均为 4 个生活步骤、0 次英语开口。以上使用文字输入，不是语音端到端验收。
- 已检查 390×844 与 360×640 手机视口。杯型、堂食/外带、外带成品、拿铁堂食与美式堂食画面中的物品标记与对应杯子对齐。当前问题切换后，上一问的气泡不会继续挡住物品；完整对话保留在“全部对话”。截图见 `qa-evidence/coffee-*.png`。
- 五张咖啡 WebP 均为 720×1280，约 78–93 KB，逐一 HTTP 200；堂食拿铁使用陶瓷杯成品图，美式堂食强调黑咖啡，避免用拉花拿铁代替美式。
- 咖啡说话者统一为 Mia；每步把已选饮品、杯型、喝法传给对话上下文。看过物品旁英文词的回答记作有词语帮助，不能计为无提示独立表达。
- 本轮浏览器错误日志未见页面脚本错误。测试中关闭麦克风、长时间用文字输入时，上游返回 `AudioServerNoAudioInputTooLongError`；文字链路仍完成。这不证明真人收音、识别准确率或插话体验已经验收。
- 未增加付费入口。多图与物品强调没有构成学习效果优于竞品的实验证明。

## 历史：2026-09-15 验证范围

- 麦克风追查补充：用户追问后，在正常预览与390×844尺寸测试中重新打开收音，均观察到音轨 `live`、未静音，音频帧与发送包持续增长；后一轮达到2300帧/2300包。此前笼统错误未记录底层异常，因此无法还原那次原因，也不能归因为未授权或浏览器不支持。已补充错误类型、信息与启动阶段诊断。本次证明采集/发送恢复，不等同于真人插话或整课语音验收。

- 后续 CUA 补验：机场 `Here you are.` → `Yes, it is mine.` → `A12.` 完成并从回顾进入办公室；办公室 `Maya.` → `My name is Li.` → 自动等候 → `Nice to meet you, too.` 完成。两者均为文字输入，回顾英语开口为0。375×667探索及成长页已截图检查，探索页无水平溢出。

- `npm run check` 通过；`npm test` **100/100 通过**。最新回归覆盖：开场前出声的保留与开场播放、同一问题乱序 final 的归属、已播角色回复不被迟到结果截断、服务端示范保留当前问题、控制词不计为姓名、最多两次有限沉默提示，以及回顾进入承诺的下一场景。这些是自动测试结果，不是实际麦克风识别准确率或真人插话验收。
- 本轮 CUA 已见 390px 手机宽度探索页的标题与筛选不重叠；已用文字核验“什么意思”的解释、“怎么说”的示范、“等一下”不推进，以及刷新恢复早餐第二步并保留 Milk。
- 当前需要作答的任务由语言回应推进，物品变化是结果反馈；不再用点杯、拖拽或点答案替代回应。办公室等候片段自动继续，不据此计为已听懂。完成页代码分列生活步骤、未看答案的英语回应与英语开口；文字不能计为口语。
- 本轮 CUA 已走通完整早餐文字链：中间刷新后恢复 2/3 并保留 Milk，`Here you are` 推进 3/3；在 3/3 先输入“怎么说”看到 Yes/No 示范，再输入 `No, thanks`，确认 `data-amount=enough`；最终回顾显示 **3 个生活步骤、0 次自主表达、0 次英语开口**。历史点杯路径、历史完成页操作数量和旧截图不作为本轮完成证明。机场、办公室的完整路径仍需另验。
- 当前 CUA 麦克风不可用，未验证真人在角色说话时插话的端到端表现。真实手机麦克风、语音服务、弱网、后台恢复和系统音频行为仍需实际设备验收；下方历史合成音频试验不替代当前版本的复验。
- 无付费功能、无云端进度同步；尚无长期 D1/D7 留存或教学效果实验结果。

以下为按日期保留的历史记录，历史测试数量和实现方式不是当前版本的验收结论。

## 历史：2026-09-06 对话与手机适配验证

### 当时可重复的回归检查

运行 `npm run check` 和 `npm test`。46 项测试覆盖任务语义、短回答、串题、累计字幕去重、迟到的最终识别、无文字的 VAD、回声过滤、断线字幕保留、回复超时、旧音频取消、旧 socket 隔离、过期评分、44.1/48 kHz 连续重采样、缓冲顺序及服务端 PCM 转发，新增早餐选择、真实问题归属、顺序状态、完整问句、中文解释与服务端场景同步检查。

这些测试使用真实函数配合模拟网络、时钟和浏览器依赖，不等同于云端 ASR 准确率测试。

### 当时浏览器检查

- Chrome 手机触控模拟：320×568、360×640、390×844、430×932、844×390。
- 四个主页面共 20 组：内容没有横向溢出，底栏位于应用可视范围。
- 麦克风权限拒绝后，可打开文字输入并提交中文；390×450 可视区中输入表单没有越界。这是键盘占位模拟，不是真机软键盘测试。
- 合成的 “Milk.” 经浏览器 AudioWorklet、16 kHz PCM、Node WebSocket 代理到真实语音服务，获得最终 “Milk.”、单个用户气泡及角色回复；收音保持开启，没有浏览器脚本异常。没有使用或保存真人录音。
- 角色播放期间，向同一采集链路注入 120 ms 合成宽带冲击噪声：没有新增取消事件、没有虚假用户气泡，角色整句播放完成且持续收音。这只覆盖该合成样本，不代表实际设备的声学降噪效果。

### 当时早餐与入场（2026-09-06）

- 浏览器回归：`npm run test:browser`。需要单独安装 Playwright 与 Chrome，或用 `PLAYWRIGHT_MODULE` 指定已有安装目录；不会加入生产依赖。先启动本地服务，也可用 `LUMA_TEST_URL` 指定测试地址。该检查模拟网络和权限拒绝，不调用收费语音服务。
- 验证入场前无麦克风请求、返回可退出、开始才申请权限、可记住跳过；点选 Milk 不产生虚构用户字幕、不计为开口，真实拖杯生效，More 增加液面，重开清空状态，机场页面不残留早餐控件。五种视口的场景面板不越界。
- 另用合成音频经真实浏览器采集和云端服务完成 Milk → 递杯 → More? → No, thanks.，三个目标全部推进。两条识别各保留一个用户气泡，收音持续开启，没有脚本错误。两次 Milk 首字约 0.76 秒和 1.31 秒，仅代表本次测试样本，不是延迟保证。没有使用或保存真人录音。
- 新背景手机图分别约 135 KB 和 85 KB。透明杯由 DOM/CSS 绘制、液面由状态驱动，不是完整角色拿杯或倒水视频。

### 当时入场视觉与问句修订（2026-09-06 下午）

- 入场保留全部说明和控件，去除模糊填充带，调整人物构图、标题层级和两组提示；没有改动收音链路。
- 浏览器测试增加三个场景 × 五个视口，确认内容与 CTA 可见、返回按钮不重叠、键盘 Tab/Escape 和复选框工作、开始前不请求麦克风。
- 取消角色“2–6 个词”的引导，使用完整的简单问句，续杯点明 milk/water；中文帮助解释句意，仍允许单词回答。
- 未读取到用户本轮完整对话记录。上述对话修订基于用户明确指出的例子和代码检查，不是对那段历史对话的完整复盘。

### 当时不打断模式（2026-09-06 傍晚，本地预览）

- 复用现有 PCM 缓冲，人物说话期间保持采集，在整句音频播完后按顺序发送。字幕会因此等待剩余播放时间，并非同时实时识别。
- 6 项新增回归验证缓冲字节不丢不重、在途 Yeah 等待完整角色字幕后落入单条气泡、声音候选保持原问题归属、缺少识别收尾时一次性恢复、没有当前角色响应时不发送多余取消、非练习页面不误显示连接中。
- `test-support/noninterrupting-browser.cjs` 用两条合成 WAV 经真实浏览器 AudioWorklet → 本地代理 → 豆包验证：在两次角色播放中分别说 Milk / No, thanks，麦克风帧数继续增加、播放没有取消、两条字幕最终确认，三个早餐任务完成，Luma 继续完整回应。无浏览器脚本错误，测试结束后关闭独立连接；没有使用真人录音。
- 此脚本为可选真实服务测试，需设置 `PLAYWRIGHT_MODULE`、`LUMA_MILK_WAV`、`LUMA_ANSWER_WAV`；运行 `node test-support/noninterrupting-browser.cjs`。未加入默认测试，以免自动调用收费服务。
- 本轮用户报告“重连”时，读取到的用户页面是旧脚本 v109，已退出到成长页面；服务日志无明确错误。无法从这些证据确定断线原因。新版在场景 DOM 保留 `lastVoiceFailure`、发生时间和连接尝试次数，尚未发布线上。

### 当时列出的真机验收待办

- iOS Safari、Android Chrome 的实际麦克风授权、软键盘、安全区和后台返回。
- 安静房间、外放回声、风扇、敲击、街道以及旁人说话，分别试说轻声单词、连续短句和中文求助。
- 弱网恢复后确认字幕不串题；超过缓冲容量时应出现提示。
- 浏览器 AEC/降噪由设备实现决定，旁人人声不是普通噪声；当前没有声纹识别，不能承诺只接收使用者声音或百分之百识别准确。

### 当时记录的发布边界

服务端密钥只从环境变量读取，静态文件采用白名单，付费接口加入同源检查、请求限制和并发上限。这些是基础滥用防护，不是用户登录或完整的按账号计费系统；面向大规模公开用户前仍需账号配额与费用监控。

## 历史：2026-09-07 mobile product redesign

- Final syntax check passed; `npm test` passed 81/81 tests, including learning evidence and UI bridge regressions.
- CUA verified mobile layouts at 375×667, 390×844 and 430×932, help/intro, physical breakfast interactions, reload recovery and typed completion. Text and actions do not count as spoken English.
- Visual comparison and screenshots are documented in `design-qa.md` and `qa-evidence/`.
- This pass did not verify real-device microphone audio end to end. The test browser had no usable microphone input; upstream session readiness alone is not an audio quality test.
- No paywall/payment implementation. No claim of measured retention or learning efficacy; no cloud progress sync added.

## 2026-09-20 自然寒暄与首句识别

- 入场只增加一句简短招呼；任务问题与显示、播放保持一致。直接点单仍可立即推进，没有新增强制寒暄节点。
- 常见问候和情绪回应在本地识别为日常交流，保留人物正在生成的回应，不调用额外语义接口，也不计为任务答案。其他无选择内容不再被原任务问题强行覆盖；不明确的 yes 仍需澄清杯型等事实。
- 修正加招呼后任务问题匹配，保留首题中文解释和短答案即时确认；“I am great”不会被当作办公室登记姓名。
- 豆包 Seeduplex 按当前[官方协议](https://www.volcengine.com/docs/6561/2549778)使用 `extension.asr.extra`，并将热词上下文序列化为字符串。首次连接、场景更新、重新连接采用当前场景词汇；保留中文，不强制替换转写。
- `npm run check`、266 项 `npm test`、`npm run test:browser` 通过。独立浏览器验证招呼显示/发送一致，Hi 不改订单、不取消人物回应、不重复发送任务问题。
- 实际豆包接口接受 session.create/session.update。两个独立冷启动会话输入合成语音，最终转写分别为 `Hi, a latte please.` 和 `这句话是什么意思？`。这是接口和合成音频抽查，不是真人手机口音识别准确率评测；后者仍需真机验证。

## 2026-09-23 咖啡店与家中早餐的连续学习体验

### 同日后续修正：服务优先、自由聊天与笔记 UI

- 根据用户实际对话记录修正：C02/C04 参考订单不再阻止堂食等真实选择；C03 可明确接受已经端来的杯子，不伪造错单修正证据。
- 二选一回答 Yes 先改为单项确认，单项确认后的 Yes 可落实选择，No 不自动选择另一项。提前表达的 Small 保留并确认；偏好、过去经历与本次点单区分。
- 更正虚构菜单的场景事实，按当前图片约束杯子和可提供饮品；字幕及音频播放前检查虚构菜单、未提供饮品与错误订单。普通聊天不再额外等待点单评分接口。
- 闲聊期间暂停下一步与任务催促；点单完成后保留对话，由用户点“结束对话”进入回顾。对话内隐藏任务编号与进度，保留实际订单。
- 笔记首屏变为场景复习卡与开始按钮，仅有真实改善时展示一条进步；记录与汇总默认折叠。
- 358 项单元与运行时回放通过；新增自然服务回放覆盖台词与音频事件先后、迟到响应、自由聊天、实际点单和结束操作。测试全部使用隔离状态，不替代真人麦克风验证。
- `test:notes-browser` 验证 3 个视口 × 3 种记录状态及复习入口；`test:conversation-browser` 验证 3 个视口 × 2 种点单状态、44px 顶栏控件、任务隐藏和手动结束入口。截图已人工检查，中央人物与物品保留可见。
- 原有 `test:browser` 和 `test:world-browser` 均通过。修改仅在本地，未发布 Render，未声称所有自由对话都经过真实模型验证。

### 保存与范围

- 开工前工作区干净，旧版 `c880bd2` 已保存为 `backup/pre-world-loop-20260923`，并在用户工作目录 `backups/2026-09-23/luma-before-world-loop.bundle` 保存完整历史，`git bundle verify` 通过。备份不含密钥、依赖或浏览器学习记录。
- 产品与交互规格见 `docs/WORLD-LOOP.md`。沿用既有真实场景与资源：咖啡店 C01–C04、家中早餐。新功能在本地分支 `codex/world-learning-loop`，本轮未发布 Render。

### 已接入的行为

- 首页和结束页统一使用同一规则推荐：当前小任务优先，其次到期复练，再按真实帮助条件建议重练或到家中早餐迁移。同一表达已有独立语音记录时可以直接换场景；一次有帮助的听力重练也可继续前进。所有推荐可离开或换任务。
- 咖啡与早餐普通续练均从小任务开头重新开始；旧的早餐内部节点在加载时归零，已发生的学习证据不删除。主动到期复练仍可定向一个表达。
- 帮助菜单包含重听、关键词、中文意思和完整示范，无强制层级。结算最多提供一个基于本次帮助记录的可折叠表达练习，不做无依据的语法诊断或单独评分。
- 结算查看关键词或示范只记录接触条件，不增加练习会话。同日随后匹配该能力的练习携带帮助；如果在回答前退出或仅发生技术异常，重新进入仍携带。已有实际回应后不永久递归继承；不含该目标的 C03 不消耗饮品选择的学习记录。
- 笔记显示具体表达最近两次的场景、日期和帮助条件。字幕辅助理解与答案提示辅助表达分别呈现，不用百分比或通关数推定掌握。
- 结束页正文可滚动，主按钮与离开入口固定在底部。场景内不增加学习卡片；维持紧凑对话、外置人物名和独立状态图标。

### 验证方式与边界

- `npm run check` 通过；完整 `npm test` 299 项通过，包含推荐、时间边界、旧存档迁移、帮助继承、取消重进、技术异常及实际 UI 桥接。
- 原有 `npm run test:browser` 通过：早餐状态、场景介绍、键盘与不同容器布局。
- `npm run test:world-browser` 使用独立浏览器存储、麦克风/WebSocket 替身和 API 拦截。实际点击入口、重练、换场景、查看示范和再次开始；控件结果与学习记录均被检查。截图/结果在 `qa-evidence/world-loop/`，均为受控测试记录，不是用户成绩。
- 新浏览器回归共 6 组交互、18 组布局检查。完成页测试 393×754、320×568、430×932、738×1198（桌面手机容器）的折叠与展开状态；另测首页开始、继续、重练、迁移和再次出门五种状态。操作可见、不重叠、无横向溢出；首页按钮下距与左右边距在 320 宽均为 20px、393 宽均为 24px。截图人工复核了阅读顺序和中央场景内容。
- 本轮在真实本地预览中检查了旧用户的继续任务与笔记，原有学习记录保留。未重新评测真实手机麦克风、口音识别率或长期留存；功能回归通过不能代替这些验证。
