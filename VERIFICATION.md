# 2026-09-06 对话与手机适配验证

## 可重复的回归检查

运行 `npm run check` 和 `npm test`。40 项测试覆盖任务语义、短回答、串题、累计字幕去重、迟到的最终识别、无文字的 VAD、回声过滤、断线字幕保留、回复超时、旧音频取消、旧 socket 隔离、过期评分、44.1/48 kHz 连续重采样、缓冲顺序及服务端 PCM 转发，新增早餐选择、真实问题归属、顺序状态、完整问句、中文解释与服务端场景同步检查。

这些测试使用真实函数配合模拟网络、时钟和浏览器依赖，不等同于云端 ASR 准确率测试。

## 浏览器检查

- Chrome 手机触控模拟：320×568、360×640、390×844、430×932、844×390。
- 四个主页面共 20 组：内容没有横向溢出，底栏位于应用可视范围。
- 麦克风权限拒绝后，可打开文字输入并提交中文；390×450 可视区中输入表单没有越界。这是键盘占位模拟，不是真机软键盘测试。
- 合成的 “Milk.” 经浏览器 AudioWorklet、16 kHz PCM、Node WebSocket 代理到真实语音服务，获得最终 “Milk.”、单个用户气泡及角色回复；收音保持开启，没有浏览器脚本异常。没有使用或保存真人录音。
- 角色播放期间，向同一采集链路注入 120 ms 合成宽带冲击噪声：没有新增取消事件、没有虚假用户气泡，角色整句播放完成且持续收音。这只覆盖该合成样本，不代表实际设备的声学降噪效果。

## 早餐与入场（2026-09-06）

- 浏览器回归：`npm run test:browser`。需要单独安装 Playwright 与 Chrome，或用 `PLAYWRIGHT_MODULE` 指定已有安装目录；不会加入生产依赖。先启动本地服务，也可用 `LUMA_TEST_URL` 指定测试地址。该检查模拟网络和权限拒绝，不调用收费语音服务。
- 验证入场前无麦克风请求、返回可退出、开始才申请权限、可记住跳过；点选 Milk 不产生虚构用户字幕、不计为开口，真实拖杯生效，More 增加液面，重开清空状态，机场页面不残留早餐控件。五种视口的场景面板不越界。
- 另用合成音频经真实浏览器采集和云端服务完成 Milk → 递杯 → More? → No, thanks.，三个目标全部推进。两条识别各保留一个用户气泡，收音持续开启，没有脚本错误。两次 Milk 首字约 0.76 秒和 1.31 秒，仅代表本次测试样本，不是延迟保证。没有使用或保存真人录音。
- 新背景手机图分别约 135 KB 和 85 KB。透明杯由 DOM/CSS 绘制、液面由状态驱动，不是完整角色拿杯或倒水视频。

## 入场视觉与问句修订（2026-09-06 下午）

- 入场保留全部说明和控件，去除模糊填充带，调整人物构图、标题层级和两组提示；没有改动收音链路。
- 浏览器测试增加三个场景 × 五个视口，确认内容与 CTA 可见、返回按钮不重叠、键盘 Tab/Escape 和复选框工作、开始前不请求麦克风。
- 取消角色“2–6 个词”的引导，使用完整的简单问句，续杯点明 milk/water；中文帮助解释句意，仍允许单词回答。
- 未读取到用户本轮完整对话记录。上述对话修订基于用户明确指出的例子和代码检查，不是对那段历史对话的完整复盘。

## 仍需真机验收

- iOS Safari、Android Chrome 的实际麦克风授权、软键盘、安全区和后台返回。
- 安静房间、外放回声、风扇、敲击、街道以及旁人说话，分别试说轻声单词、连续短句和中文求助。
- 弱网恢复后确认字幕不串题；超过缓冲容量时应出现提示。
- 浏览器 AEC/降噪由设备实现决定，旁人人声不是普通噪声；当前没有声纹识别，不能承诺只接收使用者声音或百分之百识别准确。

## 发布边界

服务端密钥只从环境变量读取，静态文件采用白名单，付费接口加入同源检查、请求限制和并发上限。这些是基础滥用防护，不是用户登录或完整的按账号计费系统；面向大规模公开用户前仍需账号配额与费用监控。
